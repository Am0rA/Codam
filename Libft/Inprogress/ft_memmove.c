#include "libft.h"

void *ft_memmove(void *dst, const void *src, size_t n)
{
	if (dst == src)
		return (dst);
	if (src < dst)
	{
		
	}
	return (dst);
}
///TEST ET KAYNAKLA YAZILAN AYNI OLURSA